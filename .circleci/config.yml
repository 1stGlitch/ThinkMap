version: 2
jobs:
    build:
        docker:
            - image: circleci/openjdk:8-jdk-browsers
        working_directory: ~/ThinkMap
        steps:
            - checkout
            - run: mvn package
            - store_artifacts:
                path: bukkit/target/ThinkMap-0.11-0.jar
